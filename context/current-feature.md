# Current Feature: Item Detail Drawer

## status

In Progress

## Goals

- Clicking an `ItemCard` opens a right-side slide-in drawer showing that item's full detail — this **is** the item detail view (no separate item page).
- Works on both the dashboard and the `/items/[type]` list pages.
- Drawer content (top → bottom, per `dashboard-ui-drawer.png`): header (type icon chip + title, type badge + language badge, close ✕); an **action bar** — Favorite (star, yellow when active), Pin, Copy on the left, Edit (pencil) + Delete (trash, red) right-aligned; then Description, Content (code block w/ line numbers for text types), Tags, Collections, and a Details section (Created / Updated dates).
- Feels snappy: fetch full detail on click (no page navigation), with a skeleton/loading state while fetching.
- Action buttons can be present but non-wired for now — this pass is the **detail display** only.

## Notes

- **Spec:** `context/features/item-drawer-spec.md`; visual reference `context/screenshots/dashboard-ui-drawer.png`.
- Use the **shadcn Sheet** (radix Dialog based), opening from the right. Sheet is **not installed yet** (`src/components/ui/` has alert-dialog, avatar, button, dropdown-menu, input, toast) — add it following the existing radix-primitive pattern used by `alert-dialog.tsx`/`dropdown-menu.tsx`.
- **Two-tier data fetching:** card data (title, description, tags…) stays server-fetched as today; full detail (content, collections, language, createdAt/updatedAt…) is fetched **on click** via a new API route `GET /api/items/[id]`. The query function lives in `src/lib/db/items.ts` (new, e.g. `getItemDetail(id)`) and the route calls it **with an auth check** (session-scoped; note the rest of the data layer is still demo-user-scoped — confirm scoping during `start`).
- **Client wrapper needed:** dashboard + items pages are server components, so drawer open/selected-item state lives in a new client component that wraps the card grids and manages the Sheet. `ItemCard` is currently a `<Link href={/items/${slug}}>` (placeholder nav) — this replaces that navigation with a click that opens the drawer (thread an `onClick`/selection up to the wrapper).
- Content rendering: text types show a syntax-highlighted, line-numbered code block (screenshot shows a `snippet`); pick a lightweight approach and keep the code-editor/type-specific extras out of scope (explicitly deferred by the spec).
- Testing scope: the new `getItemDetail` util + the API route's auth/validation are unit-testable (`src/lib/`, and route logic) — the Sheet/drawer UI is verified in the browser, not unit-tested.

## History

<!-- Keep this updated. Earliest to latest -->

- 2026-05-06: Initial Next.js setup (created via `create-next-app`); pushed to `git@github.com:fernandezalfred/devstash.git`.
- 2026-05-29: Dashboard UI Phase 1 complete. Initialized shadcn/ui (radix base, nova preset, Tailwind v4 CSS theme), added Button + Input components. Root layout set to dark mode by default with Geist fonts wired to `--font-sans`/`--font-mono`. Added `/dashboard` route with shell layout (`TopBar` + placeholder `Sidebar` + main area). Top bar is display-only (brand, disabled search field, New Collection / New Item buttons). Build passes; verified in browser.
- 2026-05-29: Dashboard UI Phase 2 complete. Built out the `Sidebar` against the reference screenshot: collapsible Types section linking to `/items/[slug]` with colored icons + item counts, a Favorites collections section (name + right-aligned star), an "All Collections" section (non-favorites, most-recent-first, with counts), and a bottom user/avatar area with a Settings link. Sidebar collapses inline on desktop and slides in as an overlay drawer on mobile, both driven by the top bar's panel toggle (`DashboardShell` + `useIsMobile`). Added `item-icons.ts` (lucide name → component map). Mock data: added `itemCount` to `ItemType` and reordered types so Links is last, to match the screenshot. Note: the second collections section is labeled "All Collections" (screenshot wording) rather than "Most recent" (spec wording); item type order follows the screenshot (Links last) over `project-overview.md`. Build passes.
- 2026-06-02: Dashboard UI Phase 3 complete. Built out the main area to the right of the sidebar: 4 stats cards (total items, collections, favorite items, favorite collections), a recent collections grid (`CollectionsGrid` + `CollectionCard`, background tinted by the collection's dominant item type), pinned items (`PinnedItems`), and the 10 most-recent items (`RecentItems`), with a shared `ItemRow` (type-colored left border + icon). Added `src/lib/dashboard.ts` for the dashboard data helpers/derivations over the mock data. Disabled the Next.js dev indicator (`devIndicators: false` in `next.config.ts`). Build passes.
- 2026-06-13: Prisma + Neon PostgreSQL setup complete. Prisma 7 with the `pg` driver adapter (`@prisma/adapter-pg`), client generated to `src/generated/prisma` (gitignored). `prisma.config.ts` drives migrations over a DIRECT (non-pooled) connection while the app runtime uses the pooled `DATABASE_URL` via the adapter in `src/lib/prisma.ts`. Initial schema (`20260608121136_init` migration) covers User/Account/Session/VerificationToken (NextAuth v5), ItemType, Item, Collection, ItemCollection (M:N join), and Tag, with indexes + cascade deletes. `prisma/seed.ts` seeds the 7 immutable system item types. Added `db:*` npm scripts and `.env.example` (DATABASE_URL pooled + DIRECT_URL). Added `scripts/test-db.ts` to smoke-test the connection and print row counts (`tsx scripts/test-db.ts`). Build passes.
- 2026-06-13: Seed data complete. Expanded `prisma/seed.ts` beyond the system item types to seed a demo user (`demo@devstash.io` / "Demo User", password `12345678` hashed with `bcryptjs` at 12 rounds, `isPro: false`, `emailVerified: now()`, upserted by email) plus 5 collections and 18 items owned by that user: React Patterns (3 TS snippets), AI Workflows (3 prompts), DevOps (1 snippet + 1 command + 2 links), Terminal Commands (4 commands), Design Resources (4 links) — real URLs for all link items. Seed is idempotent: the demo user's collections/items are deleted and recreated each run (ItemCollection join rows cascade). Link items use `contentType: TEXT` with `content: null` + `url` set (the `ContentType` enum has no URL variant). Added `bcryptjs` dependency. Build passes; seed verified against the dev DB and confirmed idempotent on re-run.
- 2026-06-13: Dashboard Collections complete. Replaced the mock collections in the dashboard main-area grid with live Neon data via Prisma. Added `src/lib/db/collections.ts` → `getDashboardCollections()` (scoped to the demo user until auth lands; ordered most-recently-updated first), which tallies items per type to derive each collection's distinct types (most-frequent first) and dominant-type accent color. `/dashboard/page.tsx` is now an async server component (`export const dynamic = "force-dynamic"`) that fetches collections and computes collection stats from the DB; item stats still come from mock until items are migrated. `CollectionCard` consumes the DB shape and renders a colored left accent border (`border-l-4` + dominant type color, matching the screenshot) plus per-type icons; `CollectionsGrid` and `StatsCards` now take props instead of importing mock data. Sidebar/pinned/recent sections still use mock data (out of scope). Also fixed the `pg` SSL deprecation warning by switching `sslmode=require` → `sslmode=verify-full` in `.env`/`.env.example`. Build passes; verified against the dev DB (5 seeded collections render with correct counts, accent colors, and type icons; no SSL warning).
- 2026-06-14: Dashboard Items complete. Replaced the mock pinned/recent items and item stats in the dashboard main area with live Neon data via Prisma. Added `src/lib/db/items.ts` with `getPinnedItems()`, `getRecentItems()` (10 most recent), and `getItemStats()` (total + favorite item counts), all scoped to the demo user until auth lands; each returns a `DashboardItem` shape that derives the type icon/color/slug per item. `/dashboard/page.tsx` now fetches collections, pinned items, recent items, and item stats in parallel via `Promise.all`; the stats object sources its item figures from `getItemStats()` instead of mock. `ItemRow` consumes the DB item shape (its own `formatItemDate` helper) and the pinned section hides entirely when there are no pinned items. Build passes. Merged to main and pushed; branch deleted.
- 2026-06-14: Stats & Sidebar complete. Sourced the sidebar (item types + collections) from Neon via Prisma; main-area stats were already DB-backed. Added `getSidebarItemTypes()` to `src/lib/db/items.ts` — the 7 system types with the demo user's per-type item counts, ordered to match the screenshot (links last). `dashboard/layout.tsx` is now an async server component (`force-dynamic`) that fetches sidebar types + collections and passes them through `DashboardShell` to the client `Sidebar`. Sidebar Types link to `/items/[slug]` with icons + counts; Collections show a "favorites" section (gold star, left) and "recents" (colored dot by dominant type, left), both sub-headers title-styled + lowercase, plus a "View all collections →" link to `/collections`. Added a border divider between Types and Collections, made section headers uppercase/bolder with the chevron on the left, and added `cursor-pointer` to the collapse toggles (Tailwind v4 drops the default button cursor). Seed: added an `isFavorite` flag to `SeedCollection` and marked React Patterns + Design Resources as favorites; re-seeded. Build passes; verified against the dev DB.
- 2026-06-17: Pro badge in sidebar complete. Added a `PlanBadge` (gold "Pro" / muted "Free" pill) inline next to the user name in the sidebar's bottom user area, driven by `currentUser.isPro` from `@/lib/mock-data` (same source as the adjacent name/email; renders **Pro** by default since the mock user is Pro). Also added a `ProTag` + `PRO_TYPE_SLUGS` ({"files","images"}) so the Types section shows a gold "Pro" tag in place of the item count on the `file`/`image` rows (the only Pro-gated system types per the spec's free-tier table); the other five types keep their counts. All changes in `src/components/dashboard/Sidebar.tsx`. Wiring the badge to a real auth/DB user is out of scope until auth lands. Build passes. Merged to main; branch deleted.
- 2026-06-19: Low-risk audit quick wins complete. Addressed four little/no-risk findings from the 2026-06-19 code-scanner audit. (1) N+1 fix: `getDashboardCollections` in `src/lib/db/collections.ts` now uses a nested `select` (`item → itemType → {id,name,icon,color}`) instead of `include: { itemType: true }`, so it stops refetching every column of the 7 constant system types per item; behavior (itemCount, ranked types, dominant accentColor) unchanged. (2) Deleted dead module `src/lib/dashboard.ts` (zero importers; the dashboard uses `src/lib/db/*` and `ItemRow` has its own `formatItemDate`). (3) Demo password moved out of source: `prisma/seed.ts` now reads `process.env.DEMO_USER_PASSWORD` (fallback for throwaway local dev only), documented in `.env.example` with a placeholder value. (4) Added a missing-`DATABASE_URL` guard to `prisma/seed.ts` and hoisted the existing guard in `scripts/test-db.ts` above adapter construction (removing the redundant in-`main` check). No Prisma schema/index changes, so no migration. Deferred (auth-coupled, out of scope): the `user.email`→`userId` query rewrite, `cache()`-wrapping the remaining item helpers, and the module-scope `userInitials` concern. Also reconnected `.env` to a freshly recreated Neon dev branch (the old dev DB was deleted). Build passes; verified end-to-end against the dev DB (all 5 collections render with correct accent colors; seed runs via the env-password path). Merged to main; branch deleted.
- 2026-06-24: Auth Phase 1 (NextAuth v5 + GitHub OAuth) complete. Set up NextAuth v5 (`next-auth@5.0.0-beta.31`) with `@auth/prisma-adapter`, using the split-config pattern for edge compatibility (verified current conventions via Context7). `src/auth.config.ts` holds providers only (no adapter); `src/auth.ts` adds `PrismaAdapter(prisma)` (reusing the existing `src/lib/prisma.ts` singleton), `session.strategy: "jwt"`, and `jwt`/`session` callbacks that surface `user.id`. `src/app/api/auth/[...nextauth]/route.ts` exports the handlers; `src/proxy.ts` (Next.js 16 proxy, named `export const proxy = auth(...)`, `matcher: ["/dashboard/:path*"]`) redirects unauthenticated users to NextAuth's default sign-in page with a `callbackUrl`. `src/types/next-auth.d.ts` augments `Session.user.id` (and `JWT.id`). Used the default sign-in page (no custom `pages.signIn`). Added `AUTH_SECRET`/`AUTH_GITHUB_ID`/`AUTH_GITHUB_SECRET` to `.env.example`. Verified: build passes; `/dashboard` redirects to sign-in; GitHub button hands off to GitHub OAuth with correct client_id/PKCE/redirect_uri (full GitHub login round-trip left to the user). Committed and merged with Phase 2 below.
- 2026-06-24: Auth Phase 2 (email/password credentials) complete. Added a Credentials provider on top of Phase 1 using the split pattern: `auth.config.ts` declares the provider with an `authorize: () => null` placeholder (keeps bcrypt/DB out of the edge config), and `auth.ts` overrides it (via `authConfig.providers.map`) with real validation — `prisma.user.findUnique` + `bcrypt.compare` against `passwordHash`, returning the user or null. New `POST /api/auth/register` route: Zod-validated input (name, email lowercased, password ≥8, confirm match), duplicate-email check (409), `bcrypt.hash(…, 12)`, creates the user, returns `{ success, data|error }`. Email is normalized to lowercase in both register and `authorize` so accounts/sign-in are case-insensitive. Added `zod` (per coding standards) — no migration (`User.passwordHash` already existed). Also gitignored `/.playwright-mcp/`. Verified end-to-end: build + lint pass; registration curl cases (201 create, 409 duplicate, 400 mismatch, 400 short, case-normalization dedupe); credentials sign-in yields an active session with `user.id`; `/dashboard` loads when authed and redirects to sign-in after logout; GitHub OAuth handoff still works. Both phases committed on `feature/auth-nextauth-github`, merged to main, branch deleted. (Dashboard data layer still scoped to the demo user — rewiring to the authed user is future work; a couple throwaway test users remain in the dev DB.)
- 2026-06-24: Auth Phase 3 (custom auth UI + session-backed sidebar) complete. Replaced NextAuth's default pages with custom UI and wired the sidebar's user area to the real session (verified Auth.js custom-page + client signIn/signOut conventions via Context7). New pages: `/sign-in` (server component; redirects already-authed users, reads `callbackUrl`) with a client `SignInForm` (`signIn("credentials", {redirect:false})` + inline error, GitHub button via `signIn("github")`, link to register); `/register` with a client `RegisterForm` (POSTs `/api/auth/register`, client-side password-match check, redirects to `/sign-in?registered=1`); and a protected `/profile` stub. `auth.config.ts` now sets `pages.signIn: "/sign-in"`; `src/proxy.ts` redirects to `/sign-in` (was `/api/auth/signin`) and its matcher now also guards `/profile/:path*`. Added reusable UI primitives `src/components/ui/avatar.tsx` (radix Avatar, image-or-initials + `getInitials`) and `src/components/ui/dropdown-menu.tsx` (radix DropdownMenu). New `src/components/dashboard/UserMenu.tsx` (client) renders the avatar as a dropdown trigger with a Profile link + Sign out (`signOut({callbackUrl:"/sign-in"})`); `Sidebar`/`DashboardShell`/`dashboard/layout.tsx` now thread the real `user` (new `getCurrentUser()` in `src/lib/db/users.ts` — session → authoritative DB row for fresh `isPro`), dropping the mock `currentUser` (now an unused export, left in place). Security: added `src/lib/safe-callback-url.ts` to block open redirects on the credentials/already-authed paths (only same-origin relative paths allowed; the GitHub path was already sanitized by NextAuth) — fixed a review finding. Resolutions: reversed Phase 1's default-page choice; avatar opens the dropdown with a Profile menu item → stubbed `/profile`; user area kept in the sidebar (requirements over the testing note's "top bar"). Build + lint pass; verified end-to-end in-browser (custom pages render; email/password + GitHub sign-in; wrong-password error; avatar/dropdown/profile/sign-out; register→sign-in with banner; proxy redirects to `/sign-in`; 0 console errors). Merged to main; branch deleted. (Dashboard items/collections remain demo-scoped — only the user area is real; rewiring the data layer to the authed user is future work. More throwaway test users now in the dev DB.)
- 2026-06-24: Register toast complete. Replaced the `?registered=1` query-param banner on `/sign-in` with a toast notification fired right after a successful registration. Added a lightweight client toast utility `src/components/ui/toast.tsx` — a module-level pub/sub `toast(message)` plus a single `<Toaster>` mounted once in the root layout (`src/app/layout.tsx`), so a toast fired immediately before `router.push("/sign-in")` survives the client navigation and renders on the next page (no toast lib dependency). `RegisterForm` now calls `toast("Account created! You can now sign in.")` then pushes to `/sign-in` (dropped the `?registered=1` param); `sign-in/page.tsx` dropped the `registered` searchParam and its banner block. Toast auto-dismisses after 5s, is manually dismissable, and uses an always-mounted `aria-live="polite"` region (`empty:hidden` + `pointer-events-none` container / `pointer-events-auto` card) so screen readers reliably announce it after navigation. Build passes; review fixes (5s duration + a11y region) applied. Merged to main; branch deleted. (Unrelated working-tree tooling files — `CLAUDE.md`, `.mcp.json`, `.claude/agents/`, `.claude/agent-memory/` — were intentionally left out of this commit.)
- 2026-06-27: Email verification on register complete. New email/password users must now verify their email before they can sign in. On registration the user is created with `emailVerified: null` and emailed a single-use verification link (24h TTL) via **Resend** — reuses the existing `VerificationToken` model (identifier=email, 32-byte hex token), so **no migration**. New `src/lib/verification.ts` (`createVerificationToken` deletes any prior tokens for the email then creates one; `consumeVerificationToken` is single-use — always deletes, then checks expiry → ok/expired/invalid; `buildVerifyUrl` prefers `AUTH_URL`, falls back to request origin) and `src/lib/email.ts` (lazy Resend client, `sendVerificationEmail`, `EMAIL_FROM` defaults to the sandbox `onboarding@resend.dev`, HTML body with user `name` HTML-escaped to prevent injection). New pages: `/verify-email` (server; consumes token, `updateMany` sets `emailVerified`, renders success/expired/invalid, with a resend form on failure) and `/check-email` (shown after registering, displays the address + a resend button). `RegisterForm` now redirects to `/check-email?email=…` (dropped the toast). Sign-in is **gated**: `auth.ts` throws a custom `EmailNotVerifiedError extends CredentialsSignin` (code `email_not_verified`) when credentials are valid but unverified; `SignInForm` reads `res.code` and shows "Please verify your email…" + a `ResendVerificationForm` (reusable; renders a button when the email is known, an input otherwise). New `POST /api/auth/resend-verification` (Zod email, generic 200 response — no enumeration, only sends for an existing unverified user). Register + resend routes log `sendVerificationEmail` failures. Removed the earlier "nag" approach: deleted the `VerifyEmailBanner`, reverted `getCurrentUser` to its original shape. Also fixed a pre-existing **stale-session redirect loop** (deleted user + live JWT bouncing between `/sign-in` and `/dashboard`): `/sign-in` and `/register` now use the DB-backed `getCurrentUser()` instead of the raw `auth()` JWT. Verified current Resend + Auth.js (custom credentials error code) conventions via Context7. Build + lint pass; verified end-to-end in-browser (register → check-email; unverified sign-in blocked with resend; verify link → success; verified sign-in → dashboard; redirect-loop gone). Resend uses the sandbox sender (delivers only to the account-owner address) until a domain is verified at resend.com/domains + `EMAIL_FROM` is set — confirmed delivery works (Resend `last_event: delivered/opened`). Committed on `feature/email-verification`, merged to main, branch deleted. The `scripts/delete-non-demo-users.ts` dev utility (wipes all non-demo users + their content; dry-run by default, `--yes` to execute, aborts if the demo user is missing) was added in a separate `chore:` commit. (Dashboard data layer still demo-scoped; sender domain still unverified.)
- 2026-06-27: Toggle email verification complete. Added `EMAIL_VERIFICATION_ENABLED` (default **enabled**; `"false"`/`"0"` disables) to switch the whole email-verification system on/off — useful while Resend has no verified sending domain (sandbox only delivers to the account-owner address). New `src/lib/flags.ts` → `isEmailVerificationEnabled()` (reads `process.env` per request; **server/Node only**, deliberately not imported into the edge `auth.config.ts` — only `auth.ts` and the register route import it). When **disabled**: `POST /api/auth/register` creates the user already-verified (`emailVerified: new Date()`) and skips the token + email; `src/auth.ts` `authorize` skips the `EmailNotVerifiedError` gate so sign-in isn't blocked. The register response now returns `verificationRequired` and `RegisterForm` branches on it — `/check-email` when on, toast + `/sign-in` when off (no `NEXT_PUBLIC_` duplication). Secure-by-default fallback: a missing `verificationRequired` falls through to the verification path. Documented in `.env.example`; no DB/schema change; GitHub OAuth unaffected. Build + lint pass; verified both modes in-browser (enabled/default → `/check-email` + token; disabled → `/sign-in`, auto-verified, sign-in succeeds → dashboard) and confirmed `flags.ts` is not pulled into the edge config. Committed on `feature/toggle-email-verification`, merged to main, branch deleted. Separately (environment maintenance, not in the feature diff): the Neon `development` branch had hit its TTL and was auto-deleted, so it was recreated from `production` (`br-solitary-poetry-atjh7dh7`), `.env` `DATABASE_URL`/`DIRECT_URL` repointed (kept `sslmode=verify-full`), re-seeded, and cleaned to just the demo user; the local `.env` sets `EMAIL_VERIFICATION_ENABLED=false` (gitignored).
- 2026-06-27: Forgot/reset password complete. Email/password users can now reset a forgotten password, reusing the existing `VerificationToken` model — **no migration**. New `src/lib/password-reset.ts` (`createPasswordResetToken` / `consumePasswordResetToken` / `buildResetUrl`): reset tokens namespace the `identifier` as `password-reset:<email>` (32-byte hex token, single-use, **1h TTL**) so they never collide with or clobber email-verification tokens (which store the bare email) — `consumePasswordResetToken` rejects any token whose identifier lacks the prefix, then strips it to recover the email. New `sendPasswordResetEmail` in `src/lib/email.ts` (mirrors `sendVerificationEmail`: lazy Resend client, `EMAIL_FROM`, HTML-escaped name, "expires in 1 hour" copy). New `POST /api/auth/forgot-password` (Zod email, **generic 200** regardless of account existence — no enumeration; only emails users that exist **and** have a `passwordHash`, so OAuth-only users are silently skipped) and `POST /api/auth/reset-password` (Zod token + password≥8; consumes the token single-use, `bcrypt.hash(…,12)`, `updateMany` on the user). **Review fix:** reset-password also marks the email verified on success (second `updateMany` scoped to `emailVerified: null` to preserve an existing timestamp) — otherwise an unverified user who reset their password would still be blocked by the sign-in verification gate despite proving inbox control. New pages: `/forgot-password` (client `ForgotPasswordForm`, generic "if an account exists…" confirmation) and `/reset-password` (reads `token` from query; missing/empty token → "Invalid reset link" + a "Request a new link" path; client `ResetPasswordForm` does password+confirm match + min-8, posts, then `toast` → `/sign-in`; token validity is checked at submit so opening the page doesn't burn a single-use token). `SignInForm` gained a "Forgot password?" link next to the password field. Build + lint pass; verified end-to-end against the dev DB (forgot-password 200 for real/unknown emails, 400 malformed; namespaced reset token created alongside an existing bare-email verification token with no collision; reset-password invalid-token/short-password 400, happy path 200 with hash verified, single-use reuse 400; unverified→reset now sets `emailVerified`); demo account restored to seed state afterward. Known limitations consistent with the rest of the app (out of scope): no rate limiting on `forgot-password` (same as `resend-verification`), email-send timing is a faint enumeration side-channel (same pattern), and JWT sessions aren't revoked on reset (inherent to `session.strategy: "jwt"`). Committed on `feature/forgot-password`, merged to main, branch deleted.
- 2026-06-28: Profile page complete. Replaced the `/profile` stub with a full profile page scoped to the **authenticated** user (the first feature page to read the authed user rather than the demo-scoped dashboard data layer). New `src/lib/db/profile.ts` → `getProfileStats(userId)` (total items, total collections, and a per-type breakdown across all 7 system types with zeros included, ordered links-last to match the sidebar); `src/lib/db/users.ts` gained `getProfileUser()` extending `getCurrentUser` with `createdAt` + a derived `hasPassword` flag (passwordHash != null) that gates the change-password action. The page (`src/app/profile/page.tsx`, server component, `force-dynamic`, redirects when unauthed) renders: user info card (reused `Avatar` GitHub-or-initials, name, email, Free/Pro pill, "Member since" via `Intl.DateTimeFormat`), a Usage section (items/collections cards + per-type breakdown with type-colored lucide icons from `item-icons.ts`), and an Account section. Account actions: **change password** — client `ChangePasswordForm` (only rendered when `hasPassword`) → `POST /api/account/change-password` (auth-required, Zod currentPassword + newPassword≥8, `bcrypt.compare` of current then `bcrypt.hash(…,12)`, rejects OAuth-only accounts with no passwordHash); **delete account** — client `DeleteAccountDialog` using a new minimal radix `AlertDialog` primitive (`src/components/ui/alert-dialog.tsx`), the destructive button stays disabled until the user types their exact email, then `DELETE /api/account` (auth-required, `deleteMany` on the session user so a stale JWT doesn't throw; cascades items/collections/accounts/sessions) followed by `signOut({callbackUrl:"/sign-in"})`. No schema/migration (used existing `User.passwordHash`/`createdAt` + cascade deletes). Build + lint pass; verified in-browser signed in as the demo user (info/stats/breakdown render with correct totals 4+3+5+0+0+0+6=18; delete dialog opens, email-gated confirm enables only on exact match, cancel works; change-password shows "Current password is incorrect" on a bad current password and "Password updated." on success — happy path tested with new=current so the demo password was left unchanged; demo account left in seed state, not deleted). Committed on `feature/profile-page`, merged to main, branch deleted.
- 2026-07-04: Rate limiting for auth complete. Added sliding-window rate limiting to the five auth endpoints via a reusable `src/lib/rate-limit.ts` backed by **Upstash Redis** (`@upstash/ratelimit` + `@upstash/redis`, verified current API via Context7). The module builds one `Redis` client (only when both `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN` are set) and five named `Ratelimit.slidingWindow` limiters sharing a module-scope `ephemeralCache` Map with `timeout: 1000` and distinct `ratelimit:<name>` prefixes; verified in the lib source that the prefix is baked into the identifier before the cache is keyed, so the shared Map can't bleed between limiters that key by bare IP. Exports: `checkRateLimit(name, id)` → `{ success, remaining, reset }` that **fails open** (returns success) when unconfigured **or** on any thrown error; `getClientIp(request)` (`x-forwarded-for` → `x-real-ip` → `"unknown"`); `retryAfter(reset)`/`retryPhrase(min)`; and `tooManyRequests(reset)` → 429 with `{ error: "Too many attempts. Please try again in X minutes." }` + `Retry-After` header (singular/plural aware). Limits: login 5/15min (IP+email), register 3/1h (IP), forgot-password 3/1h (IP), reset-password 5/15min (IP), resend-verification 3/15min (IP+email). The four owned routes call `checkRateLimit` early (before parse for IP-only keys; after parse for the IP+email `resend`) and return `tooManyRequests` on block. **Login** can't be a route (NextAuth owns `/api/auth/callback/credentials`), so it's gated inside `authorize` in `src/auth.ts` (keyed IP+email, checked before the DB lookup/bcrypt) which throws a new `RateLimitError extends CredentialsSignin` whose `code` packs the retry minutes as `rate_limited_<minutes>` — the only channel to the client through NextAuth's credentials error. Frontend: added an **error variant** to the toast utility (`toast(msg, "error")`, red `AlertCircle`); all five forms surface 429s/`rate_limited` as an error toast (`SignInForm` parses the minutes out of the code; the four fetch-based forms read the JSON message; the generic-response forms `ForgotPasswordForm`/`ResendVerificationForm` branch on status 429 before showing their anti-enumeration confirmation). New env vars documented in `.env.example` (optional → fail-open). No DB/schema change. Build + lint pass; verified **end-to-end against live Upstash** (the local `.env` has working credentials): each endpoint blocks at exactly its configured count (login 5→`rate_limited_11`, register 3, forgot 3, reset 5, resend 3), correct 429 message + `Retry-After`, and unit-checked fail-open + IP parsing + message singular/plural. Intentional tradeoffs (not defects): login counts successful attempts too (matches "5 attempts/15 min", keyed per-user so it can't affect others); login toast shows the true sliding-window reset (e.g. "11 minutes", not a flat 15). Committed on `feature/rate-limiting`, merged to main, branch deleted. (Local `.env` has live Upstash creds, so limiting is active in dev, not just fail-open; blank the two vars to fail-open locally. Dashboard data layer still demo-scoped.)
- 2026-07-07: Items list view complete. Added a dynamic type-filtered items page at `/items/[type]` (e.g. `/items/snippets`) — the destination the sidebar Types have always linked to. New `getItemsByType(slug)` in `src/lib/db/items.ts` resolves the plural route slug to its singular system item type (`slug.replace(/s$/,"")` → `prisma.itemType.findFirst({ name, isSystem:true, userId:null })`), then returns that type's items (demo-user-scoped, most-recently-updated first) via the existing `itemInclude`/`toDashboardItem`, plus the resolved `SidebarItemType` metadata (or `type:null` when the slug isn't a system type). New `src/components/items/ItemCard.tsx` — a card variant of `ItemRow` (type-colored `border-l-4` + icon chip, title, pin/favorite indicators, `line-clamp-2` description, tags, date). New `src/app/items/[type]/page.tsx` (server, `force-dynamic`; awaits the Next 16 `params` Promise) renders a type header (icon + name + item count) and a responsive grid (`grid-cols-1 md:grid-cols-2`), with an empty state and `notFound()` on an unknown type. New `src/app/items/layout.tsx` mirrors `dashboard/layout.tsx` to wrap the route in the shared `DashboardShell` (sidebar/topbar) and redirect unauthed users; `src/proxy.ts` matcher now also guards `/items/:path*`. No DB/schema change. Cards link to `/items/[slug]` (placeholder, same as `ItemRow` — no item-detail/drawer route yet). Build + lint pass; verified in-browser signed in as the demo user (snippets → 4-card 2-col grid with blue left borders/icons/pin indicator + shell, 0 console errors; notes → "0 items" empty state; unknown slug → 404). Deferred (consistent with existing item helpers): `getItemsByType`'s `include` refetches the shared `itemType` per item — could later use the nested-`select` optimization from the 2026-06-19 audit. Committed on `feature/item-list-view`, merged to main, branch deleted.
- 2026-07-08: Three-column items grid complete. Widened the items list view at `/items/[type]` from a two-column to a three-column responsive grid on large screens. One-line change in `src/app/items/[type]/page.tsx`: the grid classes went from `grid-cols-1 gap-3 md:grid-cols-2` to `grid-cols-1 gap-3 md:grid-cols-2 lg:grid-cols-3`, so the layout now steps 1 col (mobile) → 2 cols (`md`, ≥768px) → 3 cols (`lg`, ≥1024px); the container's `max-w-6xl` fits three cards at `gap-3`. Purely presentational (Tailwind only) — no DB/action/utility changes, so no new unit tests (existing Vitest suite stays green at 15 tests). Build + lint pass; verified in-browser as the demo user at all three breakpoints (375px → 1 col + collapsed sidebar drawer; 800px → 2×2; 1280px → 3 across with the 4th wrapping; no horizontal overflow, no new console errors on the items page). Committed on `feature/items-grid-3col`, merged to main, branch deleted. (Separately: the Vitest test-infra setup — `vitest.config.ts`, `flags`/`rate-limit`/`safe-callback-url` tests, npm scripts, and the testing-workflow doc edits — was committed beforehand on `chore/vitest-setup`, which is not yet merged to main.)
