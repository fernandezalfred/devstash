// Placeholder sidebar for Phase 1. The real navigation (item types, favorite
// and recent collections, user avatar) is built in Phase 2.
export function Sidebar() {
  return (
    <aside className="hidden w-60 shrink-0 border-r border-border bg-sidebar p-4 md:block">
      <h2 className="text-lg font-semibold text-sidebar-foreground">Sidebar</h2>
    </aside>
  );
}
